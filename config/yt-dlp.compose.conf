# yt-dlp configuration for Docker Compose
#
# Overrides the image-baked config (which uses localhost for Kubernetes).
# In Compose the bgutil sidecar is a separate container reachable by service name.
--extractor-args "youtubepot-bgutilhttp:base_url=http://bgutil-pot-provider:4416"
