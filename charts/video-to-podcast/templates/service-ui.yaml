apiVersion: v1
kind: Service
metadata:
  name: {{ include "video-to-podcast.ui.serviceName" . }}
  labels:
    {{- include "video-to-podcast.ui.labels" . | nindent 4 }}
  {{- with .Values.ui.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.ui.service.type }}
  ports:
    - port: {{ .Values.ui.service.port }}
      targetPort: {{ .Values.ui.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "video-to-podcast.ui.selectorLabels" . | nindent 4 }}
